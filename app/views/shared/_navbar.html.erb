<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="#">La Autenticación</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav ml-auto">
        <% if logged? %>
            <a class="nav-link">Tu Perfil
                <%= link_to current_user.name, user_path(current_user.id), class: 'nav_link' %>
            </a>
            <a class="nav-item active">
                <%= link_to 'Cerrar Sesión', session_path(current_user.id), method: :delete, class: 'nav-link'%>
            </a>
        <% else %>
            <a class="nav-item active">
                <%= link_to 'Registrate', new_user_path, class: 'nav-link'%>
            </a>
            <a class="nav-item active">
                <%= link_to 'Iniciar Sesión', new_session_path, class: 'nav-link'%>
            </a>
        <% end %>
        <a class="nav-link" href="#">Features</a>
        <a class="nav-link" href="#">Pricing</a>
    </div>
  </div>
</nav>